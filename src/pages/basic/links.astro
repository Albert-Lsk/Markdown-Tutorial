---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Comments from '../../components/Comments.astro';
---

<Layout
  title="链接与跳转 - Markdown 中文教程"
  description="链接决定你的 README、导航页和发布说明是不是一份真正能走通的文档。"
>
  <Header />

  <main>
    <section class="section section-hero">
      <div class="container">
        <div class="max-w-3xl">
          <p class="text-sm font-medium tracking-wider uppercase text-[var(--color-terracotta)] mb-4">
            基础语法 / 链接
          </p>
          <h1 class="font-serif-display text-[var(--text-inverse)] mb-6">链接</h1>
          <p class="text-xl text-[var(--text-on-dark)] opacity-80" style="line-height: 1.6;">
            链接不是“点这里”按钮的语法版。它应该把读者带到下一步动作，例如入门页、部署文档、仓库地址或反馈入口。
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container max-w-4xl">
        <div class="prose">
          <h2>基本语法</h2>
          <p>链接文本放在方括号里，地址放在圆括号里：</p>
          <pre><code>[部署说明](https://vite.dev/guide/static-deploy.html)</code></pre>

          <h2>效果预览</h2>
        </div>

        <div class="card my-6 space-y-3">
          <p>
            先看 <a href="/Markdown-Tutorial/intro" class="text-[var(--color-terracotta)] hover:underline">入门页</a>，确认这套站点是怎么组织内容的。
          </p>
          <p>
            准备上线时，对照 <a href="https://vite.dev/guide/static-deploy.html" class="text-[var(--color-terracotta)] hover:underline">Vite 静态部署文档</a> 检查产物和路径配置。
          </p>
          <p>
            如果你想补充示例或修正文案，直接发到 <a href="mailto:docs@example.dev" class="text-[var(--color-terracotta)] hover:underline">docs@example.dev</a>。
          </p>
        </div>

        <div class="prose">
          <h2>给链接补充标题</h2>
          <p>当你需要在悬停时解释链接用途，可以附带一个可选标题：</p>
          <pre><code>[发布记录](https://github.com/Albert-Lsk/Markdown-Tutorial "查看站点最近的内容更新")</code></pre>

          <h2>自动链接与邮箱</h2>
          <p>如果重点就是地址本身，可以使用尖括号包裹 URL 或邮箱：</p>
          <pre><code>{`<https://albert-lsk.github.io/Markdown-Tutorial/>
<docs@example.dev>`}</code></pre>

          <h2>写作建议</h2>
          <ul>
            <li>链接文字尽量说清目的，例如“部署说明”“更新记录”，少用“点击这里”。</li>
            <li>站内文档优先用能表达去向的名称，方便读者扫读，也方便后续迁移结构。</li>
            <li>只有在地址本身有意义时才直接贴裸 URL，例如仓库、镜像或回调地址。</li>
            <li>发版前最好把关键入口点一遍，尤其是中英文切换和深层页面跳转。</li>
          </ul>
        </div>
      </div>
    </section>

    <Comments slug="basic-links" />
  </main>

  <Footer />
</Layout>
