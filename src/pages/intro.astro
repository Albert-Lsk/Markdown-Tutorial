---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Comments from '../components/Comments.astro';
---

<Layout title="Markdown 入门基础 - Markdown 中文教程" description="学习 Markdown 的基础知识，了解什么是 Markdown 以及为什么要使用它。">
  <Header />

  <main>
    <section class="section section-dark">
      <div class="container">
        <div class="max-w-3xl">
          <p class="text-sm font-medium tracking-wider uppercase text-[var(--color-terracotta)] mb-4">
            入门
          </p>
          <h1 class="font-serif-display text-[var(--text-inverse)] mb-6">
            Markdown 入门基础
          </h1>
          <p class="text-xl text-[var(--text-on-dark)] opacity-80" style="line-height: 1.6;">
            了解 Markdown 是什么、为什么使用它，以及如何开始。
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container max-w-4xl">
        <div class="prose">
          <h2>Markdown 是什么？</h2>
          <p>Markdown 是一种轻量级的标记语言，可用于在纯文本文档中添加格式化元素。Markdown 由 John Gruber 于 2004 年创建，如今已成为世界上最受欢迎的标记语言之一。</p>
          <p>Markdown 的核心特点：</p>
          <ul>
            <li>专注于文字内容</li>
            <li>纯文本，易读易写，可以方便地纳入版本控制</li>
            <li>语法简单，没有什么学习成本</li>
          </ul>

          <blockquote>
            <p>Markdown 语法的首要设计目标是尽可能易读。基于这个目标，Markdown 格式的文档能够以纯文本形式原样发布，而不会看起来像被填满了标签或格式化指令。</p>
          </blockquote>

          <h2>为什么要使用 Markdown？</h2>
          <ul>
            <li><strong>Markdown 无处不在</strong> — GitHub、StackOverflow、CSDN、简书等平台都支持 Markdown</li>
            <li><strong>纯文本可移植</strong> — 几乎可以使用任何应用程序打开 Markdown 文件</li>
            <li><strong>独立于平台</strong> — 你可以在任何操作系统的任何设备上创建 Markdown 文件</li>
            <li><strong>适应未来变化</strong> — 即使应用程序停止支持，你仍可以用文本编辑器读取 Markdown 文件</li>
          </ul>

          <h2>工欲善其事，必先利其器</h2>
          <p>Markdown 入门的最佳方法就是多使用它。你甚至不需要下载任何程序，就可以使用在线 Markdown 编辑器来编写 Markdown。</p>
          <p>学习本教程时，你可以打开在线编辑器，一边学习语法一边练习。</p>

          <h2>Markdown 的工作原理</h2>
          <p>使用 Markdown 格式书写的文本存储在带有 <code>.md</code> 或 <code>.markdown</code> 扩展名的纯文本文件中。Markdown 应用程序使用解析器将 Markdown 格式的文本转换为 HTML，以便在 Web 浏览器中显示。</p>

          <h2>Markdown 有什么用？</h2>
          <ul>
            <li><strong>网站</strong> — Jekyll、GitHub Pages 等工具支持 Markdown 创建网站</li>
            <li><strong>文档</strong> — 适合创建笔记、技术文档</li>
            <li><strong>演示文稿</strong> — Remark、Cleaver 等工具支持 Markdown 幻灯片</li>
            <li><strong>邮件</strong> — Markdown Here 插件支持在邮件中使用 Markdown</li>
          </ul>

          <h2>Markdown 方言</h2>
          <p>每个 Markdown 应用程序都实现了稍有不同的 Markdown 语法，这些变体被称为"方言"。掌握你的应用程序所实现的 Markdown 语法是很重要的。</p>
        </div>
      </div>
    </section>

    <Comments slug="intro" />
  </main>

  <Footer />
</Layout>
