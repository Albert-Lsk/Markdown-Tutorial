---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Comments from '../../components/Comments.astro';
---

<Layout title="定义列表 - Markdown 中文教程" description="学习在 Markdown 中创建定义列表。">
  <Header />

  <main>
    <section class="section section-hero">
      <div class="container">
        <div class="max-w-3xl">
          <p class="text-sm font-medium tracking-wider uppercase text-[var(--color-terracotta)] mb-4">
            扩展语法 / 定义列表
          </p>
          <h1 class="font-serif-display text-[var(--text-inverse)] mb-6">定义列表</h1>
          <p class="text-xl text-[var(--text-on-dark)] opacity-80" style="line-height: 1.6;">
            创建术语和定义的列表。
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container max-w-4xl">
        <div class="prose">
          <h2>基本语法</h2>
          <p>定义列表使用以下格式：</p>
          <pre><code>术语
: 定义的第一行
: 定义的第二行</code></pre>

          <h2>效果预览</h2>
        </div>

        <div class="card my-6">
          <dl class="space-y-3">
            <div>
              <dt class="font-medium text-[var(--text-primary)]">Markdown</dt>
              <dd class="text-[var(--text-secondary)] pl-4">一种轻量级标记语言，由 John Gruber 于 2004 年创建。</dd>
            </div>
            <div>
              <dt class="font-medium text-[var(--text-primary)]">HTML</dt>
              <dd class="text-[var(--text-secondary)] pl-4">超文本标记语言，用于创建网页。</dd>
            </div>
          </dl>
        </div>

        <div class="prose">
          <h2>使用场景</h2>
          <ul>
            <li>词汇表/术语表</li>
            <li>字典条目</li>
            <li>百科全书风格的内容</li>
          </ul>

          <h2>注意事项</h2>
          <p>定义列表是扩展语法，并非所有 Markdown 处理器都支持。</p>
        </div>
      </div>
    </section>

    <Comments slug="extended-definition-lists" />
  </main>

  <Footer />
</Layout>
