---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Comments from '../../components/Comments.astro';
---

<Layout
  title="围栏代码块与高亮 - Markdown 中文教程"
  description="围栏代码块适合放可直接复制的命令、配置和示例片段，最好顺手标上语言。"
>
  <Header />

  <main>
    <section class="section section-hero">
      <div class="container">
        <div class="max-w-3xl">
          <p class="text-sm font-medium tracking-wider uppercase text-[var(--color-terracotta)] mb-4">
            扩展语法 / 围栏代码块
          </p>
          <h1 class="font-serif-display text-[var(--text-inverse)] mb-6">围栏代码块</h1>
          <p class="text-xl text-[var(--text-on-dark)] opacity-80" style="line-height: 1.6;">
            当你需要放一段真正会被别人复制执行的命令、配置或示例代码时，围栏代码块比缩进块更清楚，也更容易附带语法高亮。
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container max-w-4xl">
        <div class="prose">
          <h2>基本语法</h2>
          <p>使用三个反引号包裹代码块，后面可以直接写语言名：</p>

          <pre><code>{'```bash'}
npm install
npm run dev
npm run build
{'```'}</code></pre>

          <h2>为什么要写语言标识</h2>
          <p>语言标识会影响高亮和读者理解。写命令用 <code>bash</code>，配置常用 <code>yaml</code> 或 <code>json</code>，示例 Markdown 可以写成 <code>md</code>。</p>

          <ul>
            <li><code>bash</code> 适合命令行步骤和部署脚本</li>
            <li><code>yaml</code> 适合 GitHub Actions、站点配置等片段</li>
            <li><code>json</code> 适合接口返回或配置对象</li>
            <li><code>md</code> 适合示范 Markdown 本身</li>
          </ul>

          <h2>无语言标识符</h2>
          <p>如果你只是想保留原始文本格式，也可以省略语言名：</p>
          <pre><code>{'```'}
dist/
  index.html
  assets/
  search-index.json
{'```'}</code></pre>

          <h2>写作建议</h2>
          <ul>
            <li>示例最好能直接复制执行，少放“Hello, World!”这类跟场景没关系的占位代码。</li>
            <li>如果一段命令有前置条件，代码块上面先写清“在哪执行”“执行后会发生什么”。</li>
            <li>长代码块尽量只保留当前页面要讲的重点，避免把读者带进无关细节。</li>
          </ul>
        </div>
      </div>
    </section>

    <Comments slug="extended-fenced-code-blocks" />
  </main>

  <Footer />
</Layout>
