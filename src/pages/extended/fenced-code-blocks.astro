---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Comments from '../../components/Comments.astro';
---

<Layout title="围栏代码块 - Markdown 中文教程" description="学习在 Markdown 中使用围栏代码块实现语法高亮。">
  <Header />

  <main>
    <section class="section section-dark">
      <div class="container">
        <div class="max-w-3xl">
          <p class="text-sm font-medium tracking-wider uppercase text-[var(--color-terracotta)] mb-4">
            扩展语法 / 围栏代码块
          </p>
          <h1 class="font-serif-display text-[var(--text-inverse)] mb-6">围栏代码块</h1>
          <p class="text-xl text-[var(--text-on-dark)] opacity-80" style="line-height: 1.6;">
            使用围栏代码块创建带有语法高亮的代码区域。
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container max-w-4xl">
        <div class="prose">
          <h2>基本语法</h2>
          <p>围栏代码块以三个反引号（<code>```</code>）开头，后面可跟语言标识符来指定语法高亮：</p>

          <pre><code>{'```javascript'}
function hello() {'{'}
  console.log("Hello, World!");
{'}'}
{'```'}</code></pre>

          <h2>支持的常用语言</h2>
          <ul>
            <li><code>javascript</code> 或 <code>js</code> - JavaScript</li>
            <li><code>typescript</code> 或 <code>ts</code> - TypeScript</li>
            <li><code>python</code> 或 <code>py</code> - Python</li>
            <li><code>html</code> - HTML</li>
            <li><code>css</code> - CSS</li>
            <li><code>json</code> - JSON</li>
            <li><code>bash</code> 或 <code>shell</code> - Bash/Shell</li>
            <li><code>sql</code> - SQL</li>
            <li><code>yaml</code> 或 <code>yml</code> - YAML</li>
            <li><code>markdown</code> 或 <code>md</code> - Markdown</li>
          </ul>

          <h2>无语言标识符</h2>
          <p>如果不指定语言，代码块将不会有语法高亮：</p>
          <pre><code>{'```'}
这是一段普通文本代码
{'```'}</code></pre>
        </div>
      </div>
    </section>

    <Comments slug="extended-fenced-code-blocks" />
  </main>

  <Footer />
</Layout>
