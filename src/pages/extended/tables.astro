---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Comments from '../../components/Comments.astro';
---

<Layout title="表格语法 - Markdown 中文教程" description="学习如何在 Markdown 中创建表格。">
  <Header />

  <main>
    <section class="section section-dark">
      <div class="container">
        <div class="max-w-3xl">
          <p class="text-sm font-medium tracking-wider uppercase text-[var(--color-terracotta)] mb-4">
            扩展语法 / 表格
          </p>
          <h1 class="font-serif-display text-[var(--text-inverse)] mb-6">表格</h1>
          <p class="text-xl text-[var(--text-on-dark)] opacity-80" style="line-height: 1.6;">
            使用表格组织和展示数据，让信息结构化呈现。
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container max-w-4xl">
        <div class="prose">
          <h2>基本语法</h2>
          <p>表格由管道符（|）和连字符（-）组成：</p>

          <pre><code>| 语法 | 效果 |
|------|------|
| `**粗体**` | <strong>粗体</strong> |
| `*斜体*` | <em>斜体</em> |</code></pre>

          <h2>对齐方式</h2>
          <p>通过在表头下方的连字符中添加冒号来设置对齐方式：</p>

          <pre><code>| 左对齐 | 居中 | 右对齐 |
|:------|:----:|------:|
| 内容1 | 内容2 | 内容3 |</code></pre>

          <h2>效果预览</h2>
        </div>

        <div class="card my-6 overflow-x-auto">
          <table class="w-full">
            <thead>
              <tr class="border-b-2 border-[var(--color-border-warm)]">
                <th class="text-left py-2 px-3 font-medium">左对齐</th>
                <th class="text-center py-2 px-3 font-medium">居中</th>
                <th class="text-right py-2 px-3 font-medium">右对齐</th>
              </tr>
            </thead>
            <tbody>
              <tr class="border-b border-[var(--color-border-cream)]">
                <td class="py-2 px-3">内容1</td>
                <td class="py-2 px-3 text-center">内容2</td>
                <td class="py-2 px-3 text-right">内容3</td>
              </tr>
            </tbody>
          </table>
        </div>

        <div class="prose">
          <h2>实用技巧</h2>
          <ul>
            <li>可以使用快捷方式减少语法：`-:` 为右对齐，`:-:` 为居中，`:-` 为左对齐</li>
            <li>表格前后不需要空行</li>
            <li>单元格内可以使用链接、粗体、斜体等格式</li>
          </ul>
        </div>
      </div>
    </section>

    <Comments slug="extended-tables" />
  </main>

  <Footer />
</Layout>
