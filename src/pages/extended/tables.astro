---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Comments from '../../components/Comments.astro';
---

<Layout
  title="表格与状态面板 - Markdown 中文教程"
  description="表格适合放状态、负责人和时间点这类扫描型信息，不适合塞成长段正文。"
>
  <Header />

  <main>
    <section class="section section-hero">
      <div class="container">
        <div class="max-w-3xl">
          <p class="text-sm font-medium tracking-wider uppercase text-[var(--color-terracotta)] mb-4">
            扩展语法 / 表格
          </p>
          <h1 class="font-serif-display text-[var(--text-inverse)] mb-6">表格</h1>
          <p class="text-xl text-[var(--text-on-dark)] opacity-80" style="line-height: 1.6;">
            表格最擅长呈现“页面状态、负责人、发布时间”这种需要横向对比的信息。它应该帮助扫读，而不是把整段解释塞进格子里。
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container max-w-4xl">
        <div class="prose">
          <h2>基本语法</h2>
          <p>表格由管道符和分隔线组成，适合记录站点改版进度或发布清单：</p>

          <pre><code>{`| 页面 | 状态 | 负责人 |
|------|:----:|-------:|
| 首页 | 已完成 | Albert |
| Intro | 校对中 | Docs |
| 单独语法页 | 进行中 | You |`}</code></pre>

          <h2>对齐方式</h2>
          <p>你可以在分隔线里用冒号控制对齐。左侧更适合名称，中间适合状态，右侧适合日期或优先级：</p>

          <pre><code>{`| 页面 | 状态 | 优先级 |
|:-----|:----:|------:|
| Headings | 已重写 | 高 |
| Links | 已重写 | 高 |
| Images | 待补充 | 中 |`}</code></pre>

          <h2>效果预览</h2>
        </div>

        <div class="card my-6 overflow-x-auto">
          <table class="w-full">
            <thead>
              <tr class="border-b-2 border-[var(--color-border-warm)]">
                <th class="text-left py-2 px-3 font-medium">页面</th>
                <th class="text-center py-2 px-3 font-medium">状态</th>
                <th class="text-right py-2 px-3 font-medium">优先级</th>
              </tr>
            </thead>
            <tbody>
              <tr class="border-b border-[var(--color-border-cream)]">
                <td class="py-2 px-3">Headings</td>
                <td class="py-2 px-3 text-center">已重写</td>
                <td class="py-2 px-3 text-right">高</td>
              </tr>
              <tr class="border-b border-[var(--color-border-cream)]">
                <td class="py-2 px-3">Links</td>
                <td class="py-2 px-3 text-center">已重写</td>
                <td class="py-2 px-3 text-right">高</td>
              </tr>
              <tr>
                <td class="py-2 px-3">Images</td>
                <td class="py-2 px-3 text-center">待补充</td>
                <td class="py-2 px-3 text-right">中</td>
              </tr>
            </tbody>
          </table>
        </div>

        <div class="prose">
          <h2>写作建议</h2>
          <ul>
            <li>一格里尽量只放一个信息点，例如状态、日期、负责人，不要把完整说明塞进去。</li>
            <li>列数越少越容易在手机上阅读，真的很宽的表格就接受横向滚动，不要硬挤。</li>
            <li>如果内容更像步骤或结论，优先考虑列表和段落，别为了“整齐”滥用表格。</li>
            <li>状态类表格最好统一用词，例如一直用“已完成 / 进行中 / 待处理”，方便扫读。</li>
          </ul>
        </div>
      </div>
    </section>

    <Comments slug="extended-tables" />
  </main>

  <Footer />
</Layout>
