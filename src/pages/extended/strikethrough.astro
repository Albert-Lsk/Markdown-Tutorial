---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Comments from '../../components/Comments.astro';
---

<Layout title="删除线 - Markdown 中文教程" description="学习在 Markdown 中使用删除线标记已删除的文本。">
  <Header />

  <main>
    <section class="section section-dark">
      <div class="container">
        <div class="max-w-3xl">
          <p class="text-sm font-medium tracking-wider uppercase text-[var(--color-terracotta)] mb-4">
            扩展语法 / 删除线
          </p>
          <h1 class="font-serif-display text-[var(--text-inverse)] mb-6">删除线</h1>
          <p class="text-xl text-[var(--text-on-dark)] opacity-80" style="line-height: 1.6;">
            使用删除线标记已删除或不再正确的文本。
          </p>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container max-w-4xl">
        <div class="prose">
          <h2>基本语法</h2>
          <p>在文本前后各添加两个波浪号（<code>~~</code>）：</p>

          <pre><code>~~这是被删除的文本~~</code></pre>

          <h2>效果预览</h2>
        </div>

        <div class="card my-6">
          <p class="text-lg">
            正确的信息是：<span class="text-[var(--color-terracotta)]">这是正确的</span><br />
            错误的信息是：<span class="line-through text-[var(--text-tertiary)]">这是错误的</span>
          </p>
        </div>

        <div class="prose">
          <h2>使用场景</h2>
          <ul>
            <li>文档修订标记</li>
            <li>显示不再正确的旧信息</li>
            <li>待办事项中已完成的项目</li>
          </ul>
        </div>
      </div>
    </section>

    <Comments slug="extended-strikethrough" />
  </main>

  <Footer />
</Layout>
